<div id="<%= dom_id user %>">
  <div class="row bg-info bg-gradient bg-opacity-25">
    <div class="col"><%= user.name_user %></div>
    <div class="col"><%= user.email %></div>
    <div class="col"><%= user.password %></div>
    <div class="col">
      <% user.cities.each do |city| %>
        <%= city.name_city %><br>
      <% end %>
    </div>
    <div class="col"><%= link_to "Pokaż mi tego użytkownika", user, class: "btn btn-sm btn-primary" %></div>
  </div>
</div>
